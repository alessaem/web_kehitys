'use strict';

const celsius = +prompt('Enter temperature in celsius');
12;
const f = (celsius * 9) / 5 + 32;

document.querySelector(
  '#result'
).innerHTML = `${celsius} celsius astetta in fahrenheit asteina: ${f}`;
